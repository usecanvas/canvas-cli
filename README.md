# Canvas CLI

    Usage:
      canvas new [<filename>]
      canvas -h | --help
      canvas --version

    Options:
      -h, --help     Show this screen.
      --version      Show version.

## Installation

download and compile it or run it with go run :)

